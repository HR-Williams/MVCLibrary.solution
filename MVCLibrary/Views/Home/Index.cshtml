@using System.Security.Claims

@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome to the MVC Library</h1>
    <p>Learn about our MVC Library</a>.</p>
</div>


@if (User.Identity.IsAuthenticated)
{
    <!-- <h3>Hello @User.Identity.Name.Split('@')</h3> -->

     <div class="text-center">
        <h3>Hello @User.Identity.Name!</h3>
    </div>
    @using (Html.BeginForm("LogOff", "Account"))
    {
        <input type="submit" class="btn btn-default" value="Log out" />
    }
}
    

<p>@Html.ActionLink("Create an account", "Index", "Account")</p>
<p>@Html.ActionLink("Log in", "Login", "Account")</p>
<p>@Html.ActionLink("Books", "Index", "Books")</p>

