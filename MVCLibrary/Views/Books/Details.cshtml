@{
  Layout = "_Layout";
}

@model MVCLibrary.Models.Book

<h2>Book Details:</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.Title): @Html.DisplayFor(model => model.Title)</h3>

 <!-- @if (@Model.JoinEntities.Count == 0)
{
  <p>This book has no authors(lol)! Add one below.</p>
}
else
{
  <h4>This book is written by the following Author(s):</h4>
  <ul>
  @foreach (var join in Model.JoinEntities)
    {
      <li>@join.Author.FirstName + @join.Author.LastName</li>
    }
  </ul>
} -->

<p>@Html.ActionLink("Back to list", "Index")</p> 
<p>@Html.ActionLink("Edit", "Edit", new { id = Model.BookId })</p>
<p>@Html.ActionLink("Delete book", "Delete")</p>